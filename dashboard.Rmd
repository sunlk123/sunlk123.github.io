---
title: "Dashboard"
output: 
  flexdashboard::flex_dashboard:
    orientation: columns
    vertical_layout: fill
---

```{r setup, include=FALSE}
library(flexdashboard)

library(tidyverse)
library(viridis)
library(p8105.datasets)

library(plotly)
```

## Problem 2
Column {data-width=650}
-----------------------------------------------------------------------

### Chart A

```{r}
```

Column {data-width=450}
-----------------------------------------------------------------------
### Chart B

```{r}
data("rest_inspec")

rest_inspec %>%
  filter(.data = ., critical_flag == "Critical", boro == "BROOKLYN") %>%
  group_by(boro, cuisine_description) %>%
  select(.data = ., boro, critical_flag, cuisine_description) %>%
  count(critical_flag) %>%
  arrange(desc(n)) %>%
  filter(.data = ., n > 1000) %>%
  plot_ly(x = ~reorder(cuisine_description, -n), y = ~n, type = "bar") %>%
  layout(title = "Number of critical flags by cuisine type in Brooklyn, New York",
         xaxis = list(title = "Type of Cuisine",
         yaxis = list(title = "Number of critical flags")))             
```

### Chart C

```{r}
```
